# ⚡ راهنمای Momentum (برنامه‌ریز مطالعه)

یک اپ لوکال و آفلاین برای ثبت مطالعهٔ روزانه، تست‌ها، هدف‌ها و آمار — با تقویم شمسی و روزشمار کنکور. همهٔ داده‌ها روی کامپیوتر خودت می‌مونه.

> نسخهٔ قابل چاپ همین راهنما داخل اپ هم هست: دکمهٔ «؟» کنار هدر یا `http://localhost:3000/guide.html`

---

## ۱. نصب و اجرا

**پیش‌نیاز: فقط Node.js** — از https://nodejs.org نسخهٔ **LTS** (۲۲ به بالا) رو نصب کن (Next → Next → Finish). همین.

### نصب (یک بار)

روی **`install.bat`** دابل‌کلیک کن:
- چک می‌کنه Node نصب باشه (اگه نبود، صفحهٔ دانلودش رو باز می‌کنه)
- میان‌بر **«Momentum»** با آیکون ⚡ روی دسکتاپ می‌سازه (میان‌بر قدیمی داری؟ دوباره `install.bat` بزن یا Properties → Change Icon → `momentum.ico`)
- می‌پرسه با روشن شدن ویندوز خودکار اجرا بشه یا نه (بعداً هم می‌تونی از `Win+R → shell:startup` حذفش کنی)

### اجرا (هر بار)

| روش | چی می‌شه |
|---|---|
| میان‌بر **Momentum** روی دسکتاپ | در پس‌زمینه بدون پنجرهٔ سیاه اجرا می‌شه و مرورگر خودش باز می‌شه ← **ساده‌ترین** |
| `start.bat` | همون، ولی با پنجرهٔ کنسول (لاگ‌ها رو می‌بینی) |
| `stop.bat` | خاموش کردن (اگه در پس‌زمینه اجراست). داخل اپ هم: تنظیمات → «خاموش کردن Momentum» |

نکته‌ها:
- **دو بار اجرا کنی** نمونهٔ دوم ساخته نمی‌شه؛ فقط مرورگر رو روی همون اپِ باز، باز می‌کنه.
- **پورت**: پیش‌فرض ۳۰۰۰. اگه برنامهٔ دیگه‌ای گرفته باشه، خودش ۳۰۰۱، ۳۰۰۲… رو امتحان می‌کنه و مرورگر رو روی پورت درست باز می‌کنه. لازم نیست کاری کنی. با خاموش شدن، پورت آزاد می‌شه.
- اگه مرورگر خودش باز نشد: `http://localhost:3000` رو باز کن (پورت فعلی در `data\port.txt`).
- اپ رو هر جا می‌خوای بذار (مثلاً `Documents\Momentum`)؛ اگه جابه‌جاش کردی `install.bat` رو دوباره بزن تا میان‌بر درست بشه.

اجرای دستی (اختیاری): `npm start` / `npm stop`. هیچ `npm install`ای لازم نیست.

## ۲. چطور استفاده کنم؟

| تب | چی توشه |
|---|---|
| **داشبورد** | جملهٔ انگیزشی روز، روزشمار کنکور، خلاصهٔ امروز (ساعت، تست، درصد + مقایسه با دیروز)، نوار سهم درس‌ها، تایم‌لاین امروز، انجام‌شده/مانده، یادداشت روز، مقایسه با میانگین ۷ روز و رکورد، وضعیت هفته و خروجی All Plan |
| **روزانه** | ثبت و ویرایش مطالعهٔ هر روز؛ با فلش‌ها یا آیکون تقویم بین روزها جابه‌جا شو |
| **هفتگی** | ۷ ستون شنبه تا جمعه، تست و ساعت هر روز، هدف هفته (کلی و به تفکیک درس). کلیک روی هر روز → تب روزانهٔ همون روز |
| **ماهانه** | تقویم ماه با شدت رنگ بر اساس ساعت مطالعه، هدف ماه |
| **آمار** | بازهٔ دلخواه: ساعت، تست، درصد میانگین، روز مطالعه، تفکیک درس، روند روزانه و «بهترین رکوردها» |
| **ورود از اکسل** | قالب اکسل بگیر، برنامه‌های قبلی رو بریز، برگردون؛ بعد از بررسی و تأیید یک‌جا اضافه می‌شن |
| **تنظیمات و بکاپ** | اسم تو (برای سلام توی داشبورد)، تاریخ/عنوان کنکور، تم، مدیریت درس‌ها، بکاپ و بازیابی، خروجی CSV، پاک کردن تاریخچه |

### ثبت یک مطالعه (تب روزانه)

- **درس** و **مدت کل** اجباری‌اند؛ بقیه اختیاری.
- **ساعت شروع/پایان** بنویسی، مدت خودش حساب می‌شه (حتی اگه از نیمه‌شب رد بشه).
- **خواندن / مرور / تست زدن** (دقیقه) بنویسی، جمعشون می‌شه مدت کل. اگه خودت مدت کل رو دستی عوض کنی، همون می‌مونه.
- **تست**: تعداد کل، درست، غلط → درصد با نمرهٔ منفی همون لحظه نشون داده می‌شه: `(۳×درست − غلط) ÷ (۳×کل)`. تست‌هایی که درست/غلط ندارن، در میانگین درصد حساب نمی‌شن.
- **شرح**: مثلاً «فیلم جلسه ۱۲» تا یادت بمونه کجا بودی.
- **انجام شد**: می‌تونی برنامهٔ روز رو از قبل بنویسی و بعد تیک بزنی (از داشبورد هم می‌شه).

### هدف‌گذاری

در تب هفتگی/ماهانه «هدف کلی (ساعت)» رو بنویس؛ نوار پیشرفت و درصد نشون داده می‌شه. «هدف به تفکیک درس» رو باز کن تا برای هر درس جدا هدف بذاری.

### خروجی هفتگی All Plan (PDF)

**All Plan** جدول بزرگ «برنامه هفتگی انجام شده» است: هر ردیف یک هفته، هر ستون یک درس؛ داخل هر خانه شرح کارهای اون هفته (خواندن/مرور/تست) و زیرش زمان‌ها و جمع. ستون «جمع ساعات» جمع هفته و ردیف آخر جمع هر درس روی همهٔ هفته‌هاست. از منوی بالا «همهٔ هفته‌ها» یا ۴/۸/۱۲ هفتهٔ اخیر رو انتخاب کن. کنارش «گزارش تفصیلی هفته» (روز به روز) و «جدول خلاصه» هم هست.

1. از داشبورد یا تب هفتگی روی «خروجی All Plan» بزن.
2. «چاپ / ذخیره PDF» → مقصد (Destination) رو **Save as PDF** بذار → Save.
3. با «هفتهٔ قبل/بعد» بالای صفحه گزارش هفته‌های دیگه رو هم بگیر.

### ورود از اکسل (برنامه‌های قبلی)

اگه قبلاً جایی برنامه ثبت کردی، تب **«ورود از اکسل»**:

1. «دانلود قالب اکسل» → `momentum-template.xlsx`. شیت «برنامه» رو پر کن؛ شیت «راهنما» توضیح ستون‌ها، لیست دقیق درس‌ها و نمونه داره.
2. فقط **تاریخ** (۱۴۰۵/۰۶/۲۷)، **درس** (دقیقاً اسم داخل اپ) و **مدت** اجباری‌اند. مدت: دقیقه (۱۸۰) یا ساعت:دقیقه (3:00). به جای مدت کل می‌تونی خواندن/مرور/تست یا ساعت شروع و پایان بنویسی.
3. فایل (xlsx یا csv) رو در همون تب انتخاب کن → پیش‌نمایش: سبز = معتبر، قرمز = خطا (اضافه نمی‌شه)، زرد = احتمالاً تکراری/مشکوک.
4. تیک بزن و «افزودن … ردیف». قبلش بکاپ خودکار گرفته می‌شه.

داخل همون تب جدول «هر ستون چی باید باشه؟» (مثال درست/اشتباه هر ستون) و «دانلود نمونهٔ پرشده» هست.

### جدول خلاصهٔ هفته

کنار «خروجی All Plan» دکمهٔ «جدول خلاصهٔ هفته» هست: فقط یک جدول تمیز (درس، تعداد جلسه، مطالعهٔ کل، خواندن، مرور، تست زدن، تعداد تست، درست، غلط، درصد) + ردیف جمع.

### تم روز/شب

دکمهٔ کنار هدر: خودکار (طبق ویندوز) → روشن → تاریک. در تنظیمات هم قابل انتخابه.

---

## ۳. بکاپ، بازیابی و امنیت داده

**داده‌ها کجان؟**
- همه‌چیز در یک فایل SQLite: `data\plan.db` (کنار همین فایل `plan.db-wal` هم هست؛ جزو دیتاست).
- بکاپ خودکار در پوشهٔ `backups\`: هر بار اجرا، هر ۶ ساعت، هنگام خروج، قبل از بازیابی و قبل از پاک کردن تاریخچه (۳۰ فایل آخر نگه داشته می‌شه).

**بکاپ دستی (توصیه: هفته‌ای یک بار)**
1. داشبورد → «بکاپ کامل» یا تنظیمات → «بکاپ کامل JSON». این فایل **همه‌چیز از روز اول** رو داره: درس‌ها، همهٔ آیتم‌ها با جزئیات، هدف‌ها، یادداشت‌های روزانه، تنظیمات.
2. فایل JSON رو یه جای امن بذار: Saved Messages تلگرام، Google Drive، ایمیل به خودت، فلش.

⚠️ اگه لپ‌تاپ خراب/گم بشه، فقط همین فایل JSON نجاتت می‌ده. پوشهٔ `backups\` روی همون لپ‌تاپه!

**بازیابی**
1. اپ رو روی سیستم جدید نصب و اجرا کن.
2. تنظیمات → «بازیابی از JSON» → فایل بکاپ رو انتخاب کن → تأیید. همه‌چیز برمی‌گرده (قبلش هم از دیتای فعلی بکاپ گرفته می‌شه).

**بازگرداندن از فایل .db (پیشرفته)**
1. سرور رو ببند.
2. یکی از فایل‌های `backups\plan-….db` رو کپی کن به `data\plan.db` و `plan.db-wal` و `plan.db-shm` رو پاک کن.
3. دوباره اجرا کن.

**خروجی اکسل**: تنظیمات → «خروجی اکسل (CSV)»؛ همهٔ آیتم‌ها با ستون‌های جدا. با Excel/Google Sheets باز می‌شه.

**پاک کردن کل تاریخچه**: تنظیمات → «منطقهٔ خطر». دو مرحله تأیید (یک تأیید + نوشتن عبارت «پاک کن»). قبل از پاک شدن، بکاپ `.db` و `.json` در پوشهٔ بکاپ ذخیره می‌شه. درس‌ها و تنظیمات پاک نمی‌شن.

---

## ۴. اشتراک‌گذاری با دوستان

**دادن اپ به دوستان**
1. پوشهٔ اپ رو **بدون** پوشه‌های `data\` و `backups\` زیپ کن (اون‌ها دیتای شخصی توئه). لازم: `server.js`، `package.json`، `install.bat`، `start.bat`، `start-hidden.vbs`، `stop.bat`، پوشه‌های `src\` و `public\`.
2. زیپ رو بفرست.
3. دوستت: Node.js نصب می‌کنه → زیپ رو باز می‌کنه → `install.bat` → میان‌بر دسکتاپ. تمام.

هر کس دیتای خودش رو داره؛ اپ چیزی به اینترنت نمی‌فرسته.

**اشتراک گذاشتن برنامه یا گزارش**
- گزارش هفتگی PDF (All Plan) رو بگیر و بفرست.
- کل دیتا: فایل بکاپ JSON رو بده؛ طرف مقابل با «بازیابی از JSON» می‌بینه (دیتای خودش جایگزین می‌شه — قبلش بکاپ بگیره).

**چند دستگاه**: اپ همگام‌سازی خودکار نداره. کل پوشه رو داخل Google Drive/OneDrive بذار و هر بار فقط روی یک دستگاه اجرا کن، یا با JSON جابه‌جا کن.

**دسترسی از گوشی (روی همان وای‌فای)**: پیش‌فرض فقط از همون کامپیوتر باز می‌شه. برای گوشی، در `server.js` عبارت `'127.0.0.1'` رو به `'0.0.0.0'` تغییر بده و در گوشی `http://IP-کامپیوتر:3000` رو باز کن (IP با `ipconfig`).

---

## ۵. سؤالات متداول و رفع مشکل

- **صفحه نمیاد / This site can't be reached**: با `start.bat` اجرا کن تا خطا رو ببینی؛ احتمالاً Node نصب نیست یا قدیمیه (`node -v` باید ۲۲ یا بالاتر باشه). `start.bat` خودش این رو چک می‌کنه و صفحهٔ دانلود رو باز می‌کنه.
- **پورت ۳۰۰۰ مشغوله**: خودش پورت بعدی رو برمی‌داره؛ کاری لازم نیست. پورت فعلی در `data\port.txt`.
- **دو بار باز شد / می‌گه از قبل در حال اجراست**: طبیعیه؛ فقط یک نمونه اجرا می‌شه. برای خاموش کردن `stop.bat`.
- **میان‌بر دسکتاپ کار نمی‌کنه**: پوشه رو جابه‌جا کردی؛ `install.bat` رو دوباره بزن.
- **فونت فارسی زشت شد**: فونت از Google Fonts لود می‌شه؛ آفلاین فونت سیستم استفاده می‌شه — اپ کار می‌کنه.
- **تاریخ امروز اشتباهه**: تاریخ/ساعت ویندوز رو چک کن.
- **اشتباهی چیزی حذف کردم**: از پوشهٔ `backups\` نزدیک‌ترین فایل رو با روش «بازگرداندن از .db» برگردون.
- **درس جدید / حذف درس**: تنظیمات → درس‌ها. درسی که آیتم داره حذف نمی‌شه؛ «بایگانی» کن.
- **تست‌های اجرایی** (برای برنامه‌نویس‌ها): `npm test`.
